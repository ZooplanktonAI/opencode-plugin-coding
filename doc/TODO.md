# TODO

Tracked improvements deferred from review rounds.

- TODO-001: Integrate `testDrivenDevelopment.enabled` into orchestrate flow — `skills/test-driven-development/SKILL.md` says orchestrate should modify the core-coder implementation template when TDD is enabled, but `skills/orchestrate/SKILL.md` never reads this config field. Needs design decision on how TDD modifies the orchestrate phases. (A-3)
- TODO-002: Update MiniMax key in `.opencode/reviewer-knowledge.json` — the gitignored knowledge file may contain the old model ID `alibaba-coding-plan-cn/MiniMax-M2.5` as a key. When the file exists, update the key to `minimax-cn-coding-plan/MiniMax-M2.7` to match `workflow.json`. File is ephemeral and not present on clean clones. (A-12)
- TODO-003: GitHub Enterprise custom-domain auto-detection — `detectPlatform()` only checks for `github.com` in `project.repo`. GitHub Enterprise instances with custom domains (e.g., `github.mycompany.com`) are not auto-detected and fall through to `local` mode. Users must set `platform: "github"` explicitly. Consider adding a heuristic (e.g., checking for `/api/v3` endpoint or a configurable domain list) to auto-detect GHE instances. (ADV — flagged by multiple reviewers across rounds 1–3)
- TODO-004: No-remote repo support for local mode — local mode currently requires a git remote named `origin` for `git push`/`git fetch` operations. Repos with no remote at all cannot use the orchestrate workflow. Consider supporting a fully-local mode that uses only local branches (no push/fetch), or at minimum provide a clear error message when `origin` is missing. (ADV — flagged in round 3)
